/*#include "../../../include/physics.h"
#include "../../../include/entity.h"
#include "../../../include/grid.h"

Intent lalala_ia_scorpion(Grid *grid, Entity *self) {
    Intent intent = {0};

    // O Escorpião sempre busca 4 células à sua frente
    Coordinates busca;
    busca.y = self->position.y;

    switch(self->direction){
        case 0:
            busca.x = self->position.x + 32;
            break;
        case 1:
            busca.x = self->position.x - CELL_SIZE*4;
            break;
        default:
            busca.x = self->position.x + 32;
            break;
    }

    EntityList ents = grid_query_region(grid, busca, CELL_SIZE*4, 16);

}*/
